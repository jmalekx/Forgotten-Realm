using UnityEngine;

public class Dagger : Collectible
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Dagger instance created: " + gameObject.name);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
